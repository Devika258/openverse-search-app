{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Openverse media search frontend",
  "scripts": {
    "prestart": "npx kill-port 3000",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --watchAll=false"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "react-router-dom": "^7.4.1",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "kill-port": "^2.0.1",
    "supertest": "^7.1.0"
  }
}
